{
  "framework": "vite",
  "installCommand": "cd ../.. && rm -rf node_modules packages/*/node_modules && npm install --include=optional",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
